# Used for local production builds (`npm run build`) when you are NOT using GitHub Actions secrets.
# For GitHub Pages, prefer setting repo secret VITE_API_URL to your Render root (no trailing slash).
# The deployed site also has a code fallback if this is unset/mis-set.
VITE_API_URL=https://csi-vegas.onrender.com
